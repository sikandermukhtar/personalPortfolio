/*
SANITY CMS PAUSED

Restore these imports when reconnecting the Studio:

import { blogPost } from './blogPost'
import { project } from './project'

export const schemaTypes = [blogPost, project]
*/

export const schemaTypes = []
